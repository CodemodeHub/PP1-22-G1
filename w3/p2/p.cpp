#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int n;
    cin >> n;

    cout << sqrt(n) << endl;
    int x = sqrt(n);
    cout <<  x * x << endl;
    // Дальше решайте сами
    return 0;
}